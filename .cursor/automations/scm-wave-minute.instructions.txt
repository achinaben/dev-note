你是 scm-platform 实现 Agent。本 Automation **每分钟**触发一次（cron: * * * * *），每次只执行一轮，不要问我是否继续。

## 防重 / 空转（必须先做）
0. 读 scm-platform/progress.md 与 scm-platform/AGENTS.md。
   - 若「运行日志」最后一条在 **2 分钟内**，且内容仅为「跳过」、本轮无任何代码/配置变更：写一行「跳过：距上轮过近」后结束。
   - **不要**因 W36 已完成就停止；应执行 AGENTS.md「下一步」中的 **当前波次（如 W37）**。
   - 「阻塞项」仅记录环境限制；**不要**因 JDK25/本机 Docker 未测而整轮空转——改做不依赖该环境的项（单测、文档、CI、compose 配置）。

## 启动
1. 以 AGENTS.md「下一步」为任务源；progress.md 中对应波次清单第一项未勾选项为准。
2. 工作目录：scm-platform（Java 17；Windows 设 JAVA_HOME 到 IDEA JBR）。

## 执行
3. 本轮只做 **一条** 未完成项；禁止改 业务方案/ai-dev/contracts 字段。
4. 改代码后执行 `mvn test`（可 `-pl` 单模块避免 OOM）；未全绿则修或写清阻塞项。
5. 在 progress「运行日志」追加：时间、做了什么、测试结果。

## 波次完成
6. 当前波次清单全勾后：更新 AGENTS.md（标完成 + 写出下一波 3 条以内「下一步」），progress「目标波次」+1。

## 停止条件
7. 仅当：需用户二选一决策、或本轮已做完一条且有明确下一条说明，才结束。
8. 结束时中文 3～5 条 bullet：完成项/跳过原因、测试情况、下次从哪项继续。
