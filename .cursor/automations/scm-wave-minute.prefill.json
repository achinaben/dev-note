{
  "name": "SCM 波次推进",
  "description": "每分钟触发：读 AGENTS.md 推进 scm-platform 波次；mvn test；更新 progress.md。工作区：开发-note。",
  "workflow": {
    "triggers": [
      {
        "cron": {
          "cron": "* * * * *"
        }
      }
    ],
    "actions": [],
    "prompts": [
      {
        "prompt": "scm-platform Agent，每分钟一轮。仓库 achinaben/dev-note，必须先 cd scm-platform（云端 /agent 若空则写阻塞并结束）。禁止用 gh。读 progress.md+AGENTS.md，做 W37 一条，mvn test，更新 progress。Git：只 push 到固定分支 cursor/scm-wave，禁止每轮新建 cursor/agent-*，禁止 push main。3～5 条中文 bullet。"
      }
    ],
    "model": "",
    "gitConfig": {
      "repo": "achinaben/dev-note",
      "branch": "cursor/scm-wave"
    },
    "agentOptions": {
      "skipInstall": false
    },
    "memoryEnabled": true
  }
}
