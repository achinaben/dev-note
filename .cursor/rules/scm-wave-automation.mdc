---
description: scm-platform 波次 Automation 运行时遵守 AGENTS.md 与 progress 防重逻辑
alwaysApply: true
---

# SCM 波次 Automation 规则

当任务涉及 **scm-platform** 或 **Automation「SCM 波次推进」** 时：

1. 先读 `scm-platform/AGENTS.md` 与 `scm-platform/progress.md`。
2. 每轮只做一条未完成项；改代码后 `cd scm-platform && mvn test` 全绿再继续。
3. 禁止修改 `业务方案/ai-dev/contracts` 中契约字段。
4. Automation 须在 Cursor「Automations」面板启用（仓库 prefill 不会自动跑）；定时为每分钟 `* * * * *`。2 分钟内仅「跳过」且无变更才可空转结束；跟 AGENTS「下一步」推进，勿因旧波次已完成而停止。
5. 不要问用户「是否继续」；用 progress.md 运行日志留痕。
6. **Git 仅个人工作分支模式**：每轮开始先在仓库根 **pull `cursor/scm-wave`**（本机 push 后下轮必须先拉再 run）；结束只 push 该分支；禁止每 Run 新建 `cursor/agent-*`；禁止 push/merge `main`。Cloud 须 **Work on current branch** + 分支 `cursor/scm-wave`（分支专题 2.5、2.9 节）。
