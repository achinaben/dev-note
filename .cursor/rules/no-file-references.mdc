---
description: 全局约束：禁止以引用其他文件代替正文（规则与笔记自包含）
alwaysApply: true
---

# 禁止文件引用

## 适用范围

本规则对本仓库内所有对话与编辑默认生效。

## 规则说明

1. **编写或修改 `.cursor/rules` 规则文件时**  
   不要在规则正文中使用 Cursor 的 `@路径/文件` 等方式把其他文件“嵌进来”。规则应自包含：该写什么就写进本条规则，不依赖外部文件片段。

2. **编写或修改仓库内 Markdown 笔记时**  
   不要用「见 `xxx.md`」「参考同目录下某文件」或仓库内相对路径链接，把理解负担转移到别的文件。必要信息应写在本篇内（可摘要、分点、短表）；若必须关联主题，用**文字标题/关键词**说明即可，不附可点击的仓内文件路径。

3. **与用户说明问题时**  
   不以「自己去看某某文件」作为主要回答；先给出结论与要点。若用户明确要求对照某路径，再按用户要求提及。

## 例外

- 用户**明确**要求列出路径、做目录索引、或必须跳转某文件时，不受第 2、3 条限制。  
- 用户全局偏好要求使用带路径的代码引用格式时，**代码引用**仍按该偏好执行，与本条「禁止用引用文件代替正文」不冲突。
